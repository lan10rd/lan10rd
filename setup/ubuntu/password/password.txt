sudo passwd <user>

will let you forcefully change any password, you can also do so without sudo but it will be at the constraints of the system.  i think keychains will also need to manually be fixed.